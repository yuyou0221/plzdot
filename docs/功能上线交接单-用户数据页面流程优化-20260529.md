# 用户数据页面流程优化上线交接单

用途：本交接单用于开发中控评估是否将当前功能分支合并到 `staging`，以及是否可以部署到服务器。

## 基本信息

```text
模块名称：用户数据
开发分支：feature/user-data-page-flow-20260529
本轮目标：
1. 将用户数据从单页工作台调整为多页面流程，降低阅读和操作压力。
2. 增加用户数据概览、人员名单、团队结构、建模产能、请假 / 不可排期、外包供应商、Excel 导入、模块读取视图等页面入口。
3. 保持用户数据的更新入口为 Excel 覆盖导入，不新增手动业务更新流程。
4. 让其他模块侧边栏按当前账号权限决定是否显示“用户数据”入口。
5. 保持 /users 页面自身的权限拦截，未授权账号直接访问时仍显示无权限提示。

是否已经提交：否，本轮只整理交接文档，等待总控提交
是否已经 push 到远端：否
交接总结文档路径：docs/功能上线交接单-用户数据页面流程优化-20260529.md
本地验证地址：http://127.0.0.1:3003
服务器目标：http://101.133.16.14/
```

## 改动范围

```text
改动文件清单：
- src/app/users/page.tsx
- src/app/users/user-data-page.tsx
- src/app/users/availability/page.tsx
- src/app/users/import/page.tsx
- src/app/users/modeling/page.tsx
- src/app/users/module-views/page.tsx
- src/app/users/people/page.tsx
- src/app/users/people/[id]/page.tsx
- src/app/users/teams/page.tsx
- src/app/users/vendors/page.tsx
- src/components/users/user-data-portal.tsx
- src/components/imports/import-preview-workbench.tsx
- src/components/modeling/modeling-schedule-board.tsx
- src/components/product-guide/product-guide-workbench.tsx
- src/components/product-guide/project-analysis-page.tsx
- src/components/schedule/schedule-workbench.tsx

新增 / 修改 API：
- 本轮没有新增 API。
- 用户数据页面继续复用现有接口：
  - POST /api/users/import-excel/preview
  - POST /api/users/import-excel
  - GET /api/users/export-excel
  - DELETE /api/users/people/:id

新增 / 修改数据库结构：
- 本轮没有新增或修改数据表、字段、索引。

是否包含 Prisma migration：否

是否影响登录 / 权限：
- 影响页面入口展示规则。
- 继续复用现有 canAccessUserData 规则。
- permissionLevel=0 的账号可以看到用户数据入口。
- 非 0 等级账号只有业务岗位 / 业务角色属于人力资源、人事、HR、human resources 时可以看到用户数据入口。
- 不满足规则的账号，在项目排期、建模排期、产品组工作指引、数据导入等页面侧边栏不再显示“用户数据”按钮。
- /users 页面仍保留服务端权限拦截，直接访问也不会泄露用户数据。

是否影响排期内核：否

是否影响其他模块：
- 不影响项目排期计算逻辑。
- 不影响建模排期计算逻辑。
- 不影响产品组工作指引业务逻辑。
- 只调整以下页面的侧边栏入口可见性：
  - 项目排期
  - 建模排期
  - 数据导入
  - 产品组工作指引
  - 产品组项目分析页
```

## 页面入口

```text
用户数据总览：/users
Excel 导入：/users/import
人员名单：/users/people
人员详情：/users/people/:id
团队结构：/users/teams
建模产能：/users/modeling
请假 / 不可排期：/users/availability
外包供应商：/users/vendors
模块读取视图：/users/module-views
```

## 验证情况

```text
npm run lint：通过
npm run build：通过

页面验证：
- http://127.0.0.1:3003/users 可打开用户数据总览。
- /users/people 可打开人员名单。
- /users/teams 可打开团队结构。
- /users/modeling 可打开建模产能。
- /users/availability 可打开请假 / 不可排期。
- /users/vendors 可打开外包供应商。
- /users/import 可打开 Excel 导入页面。
- /users/module-views 可打开模块读取视图。
- 从总览点击“查人员”可跳转到人员名单。
- 从人员名单点击“查看”可进入人员详情页。
- 在模块读取视图点击“建模排期”可看到建模师、稳定外包、每周可用工作日等实际读取数据。

真实数据 / 样本验证：
- 使用本地数据库现有 admin / 0000 验证页面。
- admin 账号可以看到“用户数据”侧边栏入口。
- 临时创建普通 viewer 测试账号后验证：
  - 项目排期页面不显示“用户数据”入口。
  - 建模排期页面不显示“用户数据”入口。
  - 产品组工作指引页面不显示“用户数据”入口。
  - 数据导入页面不显示“用户数据”入口。
  - 直接访问 /users 会看到“当前账号没有用户数据页面权限”。
- 临时测试账号已从本地数据库删除。

浏览器控制台是否有报错：
- 多个用户数据页面重新打开后未发现新增有效 error。
- 曾有一个 2026-05-28 的旧 React #418 日志，本轮刷新验证没有新增同类错误。

接口是否验证：
- GET /api/health 返回 200。
- 本轮没有改导入 / 导出接口，未执行真实 Excel 覆盖导入，避免覆盖本地数据。
```

## 上线要求

```text
需要上线的环境：staging，之后由中控决定是否部署服务器。

是否需要执行数据库迁移：否

是否需要执行 prisma generate：否

是否需要重启服务：需要
- 本轮是 Next.js 页面和前端组件变更，需要重新构建并重启服务。

是否需要导入 Excel 或初始化数据：否
- 本轮不要求导入 Excel。
- 如果上线后要执行真实用户数据覆盖导入，请先备份数据库。

是否需要备份数据库：
- 本轮页面优化本身不需要。
- 如果同时在服务器执行 Excel 覆盖导入，建议先备份数据库。

依赖前置能力：
- 当前目标分支需要已经包含上一轮用户数据登录、权限等级、Excel 覆盖导入、用户数据导出、模块读取视图数据整理等能力。
```

## 风险与遗留

```text
已知风险：
- 本轮新增了多个未跟踪页面目录，总控提交时需要确认 git add 包含新增文件。
- 旧的 src/components/users/user-data-workbench.tsx 已删除，当前 /users 页面统一使用新版 user-data-portal。
- Excel 覆盖导入没有在本轮重复执行真实导入测试，避免误覆盖本地数据。
- 浏览器插件在登录页输入框测试时受虚拟剪贴板限制，本轮侧边栏权限验证改用已登录 HTTP 会话完成。

未完成事项：
- 本轮只优化用户数据页面流程和入口可见性。
- 未新增新的数据库字段。
- 未新增新的导入模板字段。
- 未新增新的业务联动写入。
- 用户数据和产品组工作指引、建模排期、项目排期的正式业务联动仍由后续模块按边界读取主数据推进。

需要中控确认的问题：
- 当前 staging 是否已经包含上一轮用户数据权限和登录能力。
- 是否由中控直接提交当前分支，还是先要求功能分支开发者提交后再合并。
- 服务器部署时是否同时执行真实 Excel 覆盖导入。如果执行，需先备份数据库。

回滚方式：
- 回滚本分支页面和侧边栏改动即可。
- 因本轮没有数据库迁移，代码回滚不需要数据库回滚。
- 旧用户数据工作台组件仍在仓库中，必要时可以临时恢复到旧 /users 页面。
```

## 最简版

```text
开发分支：feature/user-data-page-flow-20260529
改了什么：把用户数据改成多页面流程，并让无权限账号在其他模块侧边栏看不到“用户数据”入口。
影响哪些模块：用户数据页面；项目排期、建模排期、产品组工作指引、数据导入、项目分析页的侧边栏入口展示。
是否有数据库迁移：没有。
lint 是否通过：通过。
build 是否通过：通过。
页面是否验证过：验证过，本地 http://127.0.0.1:3003 可打开并测试主要页面。
是否需要部署服务器：需要中控合并 staging 后重新构建并重启服务。
已知风险：新增页面文件未提交，真实 Excel 覆盖导入本轮未执行。
```
