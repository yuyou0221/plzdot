# 项目管理平台-当前总控上下文

最后更新：2026-05-27

这份文档用于新对话快速接上项目背景。新对话开始时，先阅读本文件，再阅读对应模块的任务上下文。

## 项目目标

搭建一个公司内部项目管理平台，辅助管理层和各团队做项目规划、动态排期、产能分配、任务跟进、财务测算、知识库与送审检查。

首版不是面向全公司正式上线，而是先给少数人试用，用真实数据跑通关键流程，再逐步扩展。

## 技术路线

- 前端：Next.js + React + TypeScript
- 样式：Tailwind CSS，当前为工作台式布局
- 数据库：本地 PostgreSQL + Prisma
- 本地开发地址：http://127.0.0.1:3000/
- 当前项目目录：`C:\Users\yuyou\Documents\Codex\2026-05-21\new-chat\project-management-platform`

常用命令：

```powershell
npm run dev
npm run lint
npm run build
npm run db:local:start
npm run db:import:actual -- .local\actual-run-20260525-v10\project-task-estimates-v5.json
```

## 当前数据状态

当前页面已经接入本地数据库，不再只是样例数据。

已导入的真实测算数据：

```text
.local/actual-run-20260525-v10/project-task-estimates-v5.json
```

当前最新排期测算批次：

```text
actual-import-1779649008073
```

当前真实项目数：44 个。

## 公司组织

已确认团队：

- 产品团队
- 原画团队
- 建模团队
- 平面设计团队
- 样品团队
- 新媒体运营团队
- 商务团队
- 供应链团队

产品团队中通常是 2 人一组：

- 产品研发
- 产品研发美术

产品组负责推进项目，与版权方、原画、建模、工厂等沟通，提需求、验收、检查并确保项目质量。

## 已确认核心模块

后续建议按模块拆新对话实现：

1. 项目规划
   每年根据市场状况、项目优质程度做下一年度项目规划。项目规划改动会影响动态排期与财务测算。

2. 项目排期与动态预测
   根据项目排期和实际进度生成规划视图、预测视图、风险提示和项目详情。

3. 上线日历
   每年日历从春节后的第一个月到下一个春节，共 12 个月。按当月应上线项目显示，可通过拖拽调整规划。

4. 建模排期
   每个原画设计稿对应一款建模。建模按款式分配，可多人并行，也可外包。

5. 月度 / 周度任务
   根据动态排期给各组员设定月度和周度任务。月度任务可以视为四周周度任务组合。

6. 产能排期
   包含原画、建模、样品、平面设计、生产五类产能。

7. 财务测算
   项目研发周期、成本、回款进度会影响排期。项目规划或动态排期变动后，需要看到未来财务测算。

8. 知识库
   帮助同事高效做事，后续适合接入 AI。

9. 版权方送审要求检查
   每个版权方有自己的送审要求和标准，需要 AI 辅助检查是否出错。

10. 原画比稿 / 问卷调研
    当前人工记录问卷调研，后续做成网页功能减少工作量。

11. 人员名单与权限
    用户数据基础版已完成，登录与权限基础版已完成。当前已有 admin / manager / viewer 三类角色。

## 当前 P0 优先级

第一次内部上线包已确定为四个模块：

- 项目排期
- 产品组工作指引
- 建模排期
- 用户数据

建议实现顺序：

1. 用户数据基础版
2. 项目排期稳定版
3. 建模排期基础版
4. 产品组工作指引

当前完成状态：

```text
项目排期 + 上线日历：基础版已完成，仍需继续稳定拖拽持久化与重新测算
用户数据：基础版已完成
建模排期：基础版已完成
产品组工作指引：基础版已完成，下一步接入建模排期真实款式清单、审核 / 送审结果和执行事实入口
登录与权限：基础版已完成，下一步补修改密码、默认密码强制替换、审计日志和更细粒度按钮权限
```

内部上线前最低标准：

- 真实数据能稳定导入
- 四个模块页面都能跑通
- 不出现明显错误口径
- 有简单访问保护，不裸露公网，目前已通过登录与角色权限基础版实现
- 有数据备份方式
- 高风险操作先做草稿，不直接覆盖真实排期

## 当前 Git / staging 状态

当前已经建立 `staging` 分支，用于内部测试上线，不直接把未验证内容推到 `main`。

关键提交记录：

```text
分支：staging
功能准备：4158b05 chore: prepare p0 staging test
测试清单：6d17ace docs: add staging test checklist
```

最新提交以 `git log --oneline --decorate -3` 为准。

当前本地仓库还没有配置远程仓库，因此还不能推送或部署到线上测试环境。

staging 测试上线检查清单见：

```text
docs/staging测试上线检查清单.md
```

staging 环境变量示例见：

```text
.env.staging.example
```

当前已确认测试上线方向：

```text
代码仓库：GitHub 私有仓库 https://github.com/yuyou0221/plzdot
部署位置：云服务器 101.133.16.14
服务器镜像：ubuntu_26_04_x64_20G_alibase_20260509.vhd
部署分支：staging
```

当前 `staging` 已推送到 GitHub，并跟踪远程分支：

```text
origin/staging
```

当前 staging 已部署到云服务器：

```text
访问地址：http://101.133.16.14/
服务器目录：/opt/project-management-platform
PM2 应用名：project-management-staging
应用端口：3100
Nginx：80 -> 127.0.0.1:3100
数据库：服务器本机 PostgreSQL
```

当前部署已验证：

```text
/api/health 正常
未登录访问 /users 会跳转 /login
管理员登录接口正常
```

当前 staging 已导入真实排期测算数据：

```text
导入批次：actual-import-1779657649462
本地来源：.local/actual-run-20260525-v10/project-task-estimates-v5.json
服务器路径：/opt/project-management-platform/.local/actual-run-20260525-v10/project-task-estimates-v5.json
项目数：44
项目任务数：1323
排期任务结果数：1323
未来任务数：690
```

当前仍未导入到 staging 的真实数据：

```text
真实人员名单
真实建模款式数据
外包供应商真实数据
```

GitHub 与云服务器部署方案见：

```text
docs/GitHub与云服务器staging部署方案.md
```

首版可以先不做：

- 其余模块都可以暂缓
- 修改本人密码、忘记密码、审计日志、更细粒度的全模块按钮权限可以暂缓

不能出错的业务规则：

- 财务
- 排期
- 送审

管理层最关心：

- 大家工作量是否能完成
- 是否超标
- 是否有任务阻塞
- 是否有延期风险
- 是否存在异常，例如任务耗时过久或长时间没进度

## 页面与设计偏好

用户偏好：

- 希望网页美观、易读、易操作
- 喜欢插画风
- 希望后续提供两套风格：扁平风格与插画风格，可切换

产品形态判断：

- 这是内部运营工具，不应做营销式落地页
- 首页应直接是可用的工作台
- 应保持信息密度、可扫描性、明确的状态颜色和高效操作

## 已实现工程结构

核心文件：

```text
src/app/page.tsx
src/components/schedule/schedule-workbench.tsx
src/lib/sample-schedule.ts
src/lib/schedule-repository.ts
src/lib/schedule-engine/adapters.ts
src/lib/db/prisma.ts
src/app/api/schedule/board/route.ts
src/app/api/schedule/analyze/route.ts
prisma/schema.prisma
scripts/import-actual-schedule.ts
```

用户数据基础版文件：

```text
src/app/users/page.tsx
src/components/users/user-data-portal.tsx
src/lib/user-data-repository.ts
src/lib/user-data-types.ts
src/lib/user-data-mutation.ts
src/app/api/users/**
```

登录与权限基础版文件：

```text
src/app/login/page.tsx
src/app/api/auth/login/route.ts
src/app/api/auth/logout/route.ts
src/components/auth/login-form.tsx
src/components/auth/logout-button.tsx
src/lib/auth/session.ts
src/lib/auth/password.ts
src/lib/auth/permissions.ts
src/lib/auth/current-user.ts
src/lib/auth/api.ts
src/lib/auth/default-admin.ts
src/proxy.ts
prisma/migrations/20260525033000_add_user_auth_fields/migration.sql
```

建模排期基础版文件：

```text
src/app/modeling/page.tsx
src/lib/modeling-schedule-types.ts
src/lib/modeling-schedule-repository.ts
src/lib/modeling-schedule-mutation.ts
src/components/modeling/modeling-schedule-board.tsx
src/app/api/modeling/tasks/[id]/route.ts
```

产品组工作指引基础版文件：

```text
src/app/product-guide/page.tsx
src/lib/product-guide-types.ts
src/lib/product-guide-repository.ts
src/lib/product-guide-mutation.ts
src/components/product-guide/product-guide-workbench.tsx
src/app/api/product-guide/tasks/[id]/route.ts
src/app/api/product-guide/modeling-tasks/route.ts
```

本地数据库说明：

```text
docs/local-database.md
```

API 说明：

```text
docs/api-routes.md
```

模块交接文档：

```text
docs/模块任务上下文-项目排期与上线日历.md
docs/模块任务上下文-项目排期脚本优化.md
docs/平台数据结构设计草案.md
docs/P0数据字典.md
docs/现有PrismaSchema对照改造清单.md
docs/P0数据库迁移分阶段方案.md
docs/P0数据导入总方案.md
docs/核心排期内核与Excel数据接口规范.md
docs/核心排期引擎统一使用规范.md
docs/模块任务上下文-产品组工作指引.md
docs/下一步任务上下文-产品组工作指引.md
docs/下一步任务上下文-建模排期.md
docs/下一步任务上下文-用户数据.md
docs/模块交接总结-用户数据基础版.md
docs/模块交接总结-登录与权限系统.md
docs/模块交接总结-建模排期基础版.md
docs/模块交接总结-产品组工作指引基础版.md
```

## 系统输入输出关系

当前系统按四层理解：

```text
基础数据层
↓
计划 / 测算层
↓
执行工作台层
↓
反馈回写层
```

核心模块关系：

```text
用户数据 → 项目排期 / 建模排期 / 产品组工作指引
项目规划 → 项目排期规划视图
项目排期 → 产品组工作指引
项目排期 → 建模排期
产品组工作指引 → 项目任务执行事实写入
产品组工作指引 → 款式清单录入 → 建模排期
项目排期主动读取建模排期 → 款式建模完成事实 → 项目排期预测视图
项目排期预测结果 → 财务测算 / 管理层提醒
```

系统级边界：

```text
规划视图 = 管理层计划基线
预测视图 / 压力预测 = 计划基线 + 执行事实 + 产能规则后的动态测算
产品组工作指引 = 执行动作入口，可写事实，但不能改规划基线
建模排期 = 款式级建模任务的数据源
用户数据 = 人员、团队、产能、擅长类型、稳定外包的数据源
登录与权限 = 访问保护、角色判断、写接口权限边界
Excel 数据接口 = 前期批量更新输入数据的临时入口
```

## 核心排期内核与 Excel 接口

当前已确认：

```text
项目排期脚本 = 平台唯一排期测算真相
各模块只能写入执行事实，不能重新实现排期逻辑
所有模块优先读取 ScheduleRun / ScheduleProjectResult / ScheduleTaskResult
```

当前已新增统一领域层：

```text
src/lib/schedule-domain/
```

用于集中维护任务编号到里程碑、状态翻译、完成判断、任务 7-10 建模聚合、规划 / 预测取数和回归样本。

已输出 4 份 Excel 数据接口，作为前期暴力更新数据的临时入口：

```text
outputs/excel-data-interface-20260525/01-项目排期核心数据接口.xlsx
outputs/excel-data-interface-20260525/02-用户与权限数据接口.xlsx
outputs/excel-data-interface-20260525/03-建模排期数据接口.xlsx
outputs/excel-data-interface-20260525/04-产品组执行事实接口.xlsx
```

强规则：

```text
Excel 可以更新输入数据。
排期结果必须由统一排期内核重新生成。
任何模块不能自己推导第二套排期结果。
```

详细口径见：

```text
docs/核心排期内核与Excel数据接口规范.md
docs/核心排期引擎统一使用规范.md
```

## 登录与权限基础版现状

当前已经有基础登录和角色权限，不再是裸露访问。

已完成：

- `/login` 登录页。
- `POST /api/auth/login` 登录接口。
- `POST /api/auth/logout` 退出接口。
- 使用 httpOnly 签名 Cookie 保存会话。
- 未登录访问页面会跳转到 `/login`。
- 未登录访问受保护 API 返回 `401`。
- 侧边栏增加「退出登录」。
- 用户数据页增加登录名、权限角色、初始 / 重置密码字段。

默认管理员：

```text
登录名：admin
密码：admin123456
```

注意：默认管理员只适合本地初始化，后续应尽快做修改密码或在用户数据页重置密码。正式环境不能长期使用默认密码。

测试 / 正式部署环境必须配置：

```text
AUTH_SECRET
INITIAL_ADMIN_PASSWORD
```

当前角色：

| 角色 | 权限 |
| --- | --- |
| admin | 管理员，用户数据可写，业务数据可写 |
| manager | 管理者，业务数据可写 |
| viewer | 只读成员 |

当前权限口径：

- 用户数据写接口仅 `admin` 可操作。
- 项目排期、建模任务、产品组工作指引等业务写接口允许 `admin / manager`。
- `viewer` 只读。

数据库变更：

```text
User.loginName
User.passwordHash
User.authRole
User.mustChangePassword
User.lastLoginAt
```

新增迁移：

```text
prisma/migrations/20260525033000_add_user_auth_fields/migration.sql
```

已验证：

- `npx prisma migrate deploy`
- `npx prisma generate`
- `npm run lint`
- `npm run build`
- 未登录访问首页跳转 `/login`
- `admin / admin123456` 可以登录
- 退出登录后访问受保护页面会回到登录页
- 未登录调用受保护 API 返回 `401`
- 管理员会话调用用户数据写接口验证通过

还没做：

- 修改本人密码页。
- 忘记密码。
- 审计日志。
- 更细粒度的全模块按钮权限。
- 正式环境强制替换默认管理员密码。
- 产品组工作指引默认项目组绑定当前登录用户。
- 建模排期个人信息页绑定当前登录建模师。
- 建模排期管理层统计后续作为对外可读信息补齐；当前不保留独立管理层看板。

## 项目排期输入口径

项目排期必须拆成两套输入口径：

```text
规划输入：只允许总经理 / 产品总监维护
预测输入：接受产品组工作指引、建模排期、用户数据等执行事实
```

规划视图可接受的输入：

- 项目是否立项
- 项目名称
- IP / 版权方
- 产品类型
- 项目等级 / 优先级
- 项目路线
- 计划上线日期
- 计划款数 / 规格
- 是否需要三视图
- 项目组
- 产品研发
- 产品研发美术
- 初始项目状态
- 任务规则版本

预测视图 / 压力预测可接受的输入：

- 产品组工作指引写入的任务实际完成日期
- 产品组工作指引写入的预计完成日期
- 产品组工作指引写入的阻塞、备注和进度说明
- 产品组工作指引创建的款式清单
- 建模排期写入的款式分配、状态、送审、反馈、通过
- 用户数据中的人员产能、擅长类型、稳定外包

强规则：

```text
产品组工作指引可以写进度，但不能改规划。
建模排期可以写款式状态，但不能改规划。
用户数据可以改产能，但不能改规划。
项目排期负责重新测算，不直接替代管理层决策。
```

## 规划调整规则

管理层手动调整规划里程碑时：

```text
允许单独提前某个里程碑月份。
不允许单独把某个里程碑向后推。
```

允许：

```text
原建模里程碑：8月
调整后：7月
```

不允许：

```text
原建模里程碑：8月
调整后：9月
```

原因：

- 规划视图代表公司希望达成的计划基线
- 如果现实做不到，应体现在预测视图 / 风险提示 / 压力预测里
- 如果确实要推迟整体计划，应通过调整项目计划上线日期或管理层重新规划处理

规划调整可优先复用现有 `ScheduleAdjustment` 表。每次调整应记录：

- 调整人
- 调整时间
- 调整字段
- 原值
- 新值
- 调整原因
- 是否已重新测算

保存校验：

- 操作人必须是总经理或产品总监
- 计划上线日期不能为空
- 项目路线不能为空
- 单独调整里程碑时，目标月份不能晚于原规划月份
- 如果调整计划上线日期导致整体排期向后，必须记录原因
- 如果项目已上线 / 已完结，默认不允许改规划，除非走「修正历史数据」

## 建模排期基础版现状

当前 `/modeling` 已完成基础版，侧边栏入口为「建模排期」。

建模排期当前定位：

- 读取项目排期中的建模里程碑。
- 读取和展示款式级 `ModelingTask`。
- 支持建模师分配、外包标记、建模计时、成果提交和反馈记录。
- 接收产品组工作指引提交的款式清单、任务 7/10 启动事件、审核 / 送审结果。
- 给产品组输出主动事实事件，给项目排期和管理层提供可读事实。
- 不允许建模排期直接修改规划视图、项目计划上线日期或项目排期基线。

页面结构：

- `/modeling` 默认进入 `里程碑` 视图，不直接进入款式看板。
- 顶部视图包括：`里程碑`、`款式看板`、`个人信息`。
- `里程碑` 视图从项目排期测算数据中提取建模里程碑，按本月要完成、之前未完成、下个月要完成展示。
- 建模里程碑按项目排期任务编号 `7-10` 聚合。
- 项目已经进入红蜡、模具、大货、已完结等后续阶段时，历史建模里程碑不再作为当前逾期重点显示。
- `款式看板` 按人分组，而不是按状态分列；包含未分配、各建模师、外包中等分组。
- 款式看板里的建模师身份卡不显示产能数字。
- 已通过款式不会出现在活跃排期列表里，`已通过款式` 入口放在款式看板内部。
- `个人信息` 视图当前用建模师下拉选择模拟当前用户；后续应绑定当前登录建模师。

数据读取规则：

- 如果数据库存在真实 `ModelingTask`，优先显示真实款式。
- 只有没有真实款式时，才根据 `Project.styleCount` 生成虚拟款式。
- 虚拟款式会明确标注 `虚拟 / 待补充款式名`。
- 虚拟款式不能保存到数据库，拖拽仍然只是前端草稿。
- 项目级建模进度只按真实款式计算。
- 已经全部通过的项目不会继续出现在项目建模进度面板里。

基础版已支持的保存动作：

- 真实款式拖拽到建模师后保存 `modelerId`。
- 保存外包状态。
- 建模状态只读展示，由业务事件自动推进。
- 开始建模计时。
- 建模师提交成果，款式进入 `待验收`。
- 产品审核 / 送审结果通过接口写入。
- 驳回必须填写文字反馈，可附图片 / PDF / PPT 链接。
- 已通过时写入版权方过审日期。
- 记录建模反馈、工时日志和分配历史。

当前 API：

```text
PATCH /api/modeling/tasks/:id
POST /api/modeling/style-submissions
POST /api/modeling/style-confirmations
POST /api/modeling/style-start-events
POST /api/modeling/review-results
POST /api/modeling/style-cancellations
POST /api/modeling/style-reopen-events
GET  /api/modeling/product-guide-events
GET  /api/modeling/projects/:projectId/styles
GET  /api/modeling/projects/:projectId/progress
POST /api/modeling/tasks/:id/work-submissions
POST /api/modeling/tasks/:id/work-timer
```

`PATCH /api/modeling/tasks/:id` 仍是过渡期通用单款基础编辑入口，后续应继续拆成明确事件接口。

产品组工作指引长期应通过建模排期的款式提交、启动事件、审核 / 送审结果接口写入建模事实；项目排期只读取建模结果，不直接管理每个款式。

当前复用现有数据表，没有新增或修改 Prisma schema。主要复用：

```text
Project
ProjectTask
ScheduleTaskResult
ModelingTask
ModelingFeedback
ProjectModelingProgress
User
OutsourceVendor
DataImport
```

基础版已导入过真实建模排期 Excel 数据：

```text
C:\Users\yuyou\Downloads\皮特易凡建模排期系统.xlsx
C:\Users\yuyou\Downloads\建模排期系统-忍者组.xlsx
C:\Users\yuyou\Downloads\建模排期系统-迪no&厦大.xlsx
```

`C:\Users\yuyou\Downloads\建模排期系统-丹东组.xlsx` 当时只有表头，没有可导入款式行。

截至交接总结时的导入与补录状态：

- Excel 读取 93 条款式行。
- 创建 83 条 `ModelingTask`。
- 跳过 10 条，因为项目表里没有对应项目：`插画小人mini3代` 到 `插画小人mini12代`。
- 外包款式标记为 `isOutsourced = true`。
- 外包供应商先使用占位口径：稳定外包供应商待补充。
- Excel 没有写明每个款式对应哪个建模师，所以款式负责人需要后续手动录入。
- 已根据用户提供的信息手动录入 38 条款式负责人。
- 当时数据库里 `ModelingTask` 总数为 105。
- 当时仍未分配的活跃款式为 52，外包中的活跃款式为 19。

已手动录入过负责人信息的项目：

- 假面骑士1
- 无牙仔猫猫
- 水豚噜噜2
- 菊苣2
- 鲨猫

已确认并使用的建模师：

- 黄绪浩
- 史宇华
- 黄宇轩
- 郭庆云
- 冷茂华
- 方圆
- 孟凡菲
- 王雨晴
- 张丁文

建模业务规则：

```text
超载规则：当前基础展示保留；最终规则为手头工时超过 24 个工作日
未更新提醒：建模中 且 超过 3 天未更新
同一建模师同时只能有一个运行中的计时
```

建模状态：

- 待确认
- 退回补充
- 未启动
- 未分配
- 已排期
- 排队中
- 建模中
- 修改中
- 待验收
- 待送审
- 已送审
- 等反馈
- 已通过
- 外包中
- 暂停
- 取消

推荐状态流转：

```text
待确认 -> 未启动 -> 未分配 -> 已排期 / 外包中 -> 建模中 -> 待验收 -> 待送审 -> 已送审 -> 等反馈 -> 已通过
内部不通过 / 送审不通过 -> 排队中
```

特殊状态：

```text
外包中 / 暂停 / 取消
```

基础版已验证：

- `npm run lint` 通过。
- `npm run build` 通过。
- 浏览器打开 `/modeling` 无页面报错。
- 默认进入里程碑视图。
- 款式看板按建模师分组展示。
- 款式看板不显示已通过款式。
- 已通过款式入口在款式看板内部。
- 个人信息页可查看个人款式并切换建模师。
- 有真实 `ModelingTask` 时优先展示真实款式。
- 项目建模进度按真实款式计算。
- 已完成项目不再显示在项目建模进度面板。
- 款式清单提交、建模确认、退回补充和重新提交可在模拟器验证。
- 任务 7 / 10 启动事件可在模拟器验证，任务 8 / 9 会被拒绝。
- 建模师提交成果后进入 `待验收`。
- 产品审核 / 送审模拟可推进 `待送审`、`已送审`、`等反馈`、`已通过` 或驳回到 `排队中`。
- 建模工时按分钟累计，详情可查看累计工时和反馈次数。
- 项目排期读取模拟可查看任务 7 / 10 必做款式完成事实。

基础版暂未做：

- 个人信息页还没有绑定当前登录建模师。
- 产品组真实页面还没有完整接入建模排期款式提交、任务启动、审核 / 送审结果。
- 项目排期模块还没有真实消费建模排期提供的完成事实。
- 建模排期不负责触发项目预测重新测算。
- 仍有大量款式需要手动分配。
- 外包供应商还是占位口径，未细分真实供应商。
- 部分已通过款式缺少真实完成日期。
- 款式难度、预计工时、剩余工时还需要继续补充。
- 产能算法、未来两周产能缺口、稳定外包产能还没做。
- Excel 导入稳定化还没做。

对项目排期的影响：

- 当前建模排期不会直接修改项目规划视图，也不会直接修改项目计划上线日期。
- 当前已经具备项目排期可读事实：任务 7 / 10 必做款式是否全部通过、最后一款必做款式通过日期、未通过必做款式、送审 / 反馈卡住款式。
- 建模排期不主动写 `ProjectTask`，不生成“确认回写”按钮。
- 后续由项目排期主动读取 `GET /api/modeling/projects/:projectId/progress`，自行决定是否把建模视为完成、是否写执行事实、是否重新测算。

对产品组工作指引的影响：

- 产品组工作指引仍然是款式事实的长期源头。
- 当前 Excel 和手动录入属于过渡数据补齐方式。
- 长期应由产品组工作指引创建和维护款式清单。
- 建模排期需要反向暴露：哪些款式未分配、哪些款式原画未过审、哪些款式卡在送审 / 等反馈 / 修改、哪些项目建模可能影响排期、哪些款式需要产品美术验收。

对用户数据的影响：

- 建模师来源已经读取 `User` 表。
- 当前使用字段包括是否建模师、建模师姓名、每周产能、擅长类型。
- 后续用户数据模块仍需补齐建模师真实产能、擅长类型、是否在职 / 是否可排期、稳定外包供应商、外包可用产能。

## 产品组工作指引基础版现状

当前 `/product-guide` 已完成基础版，页面入口已加入侧边栏。

页面结构：

- 每次只展示一个产品项目组，支持上一项目组、下一项目组和下拉切换。
- 当前已有基础登录体系，但产品组工作指引还没有用登录用户自动识别所在组；默认项目组仍先用浏览器本地记忆。
- 顶部不展示今日需处理、本周需处理、风险数量等焦虑型数字指标卡。
- 页面上方是紧凑工具栏，搜索、产品研发、产品美术、我的视角、只看风险、只看我负责等筛选默认收起。
- 每个项目组有两个子页：`里程碑` 和 `本周工作指引`。
- `里程碑` 子页复用项目排期完整项目卡数据源，按项目排期看板尺寸展示，并支持 `规划视图 / 压力预测` 切换。
- `本周工作指引` 分为五个纵向模块：本周需完成、本周在推进、本周要开始、风险任务、版权方反馈。
- 本周任务以单行任务条展示，按项目、任务、DDL、延期线对齐。
- 详情卡默认隐藏，点击任务或里程碑项目后才显示右侧详情。

基础版已支持的执行动作：

- 标记任务完成。
- 更新预计完成日期。
- 标记阻塞。
- 解除阻塞。
- 录入建模款式清单。

当前新增 API：

```text
PATCH /api/product-guide/tasks/:id
POST /api/product-guide/modeling-tasks
```

`POST /api/product-guide/modeling-tasks` 当前作为兼容旧入口保留，内部已桥接到建模排期的款式清单提交契约；成功文案应是“已提交建模款式清单，等待建模侧确认”，而不是“已生成未分配建模款式”。

当前复用现有数据表，没有新增或修改 Prisma schema。主要复用：

```text
Project
ProjectTask
ScheduleProjectResult
ScheduleTaskResult
Alert
WorkTask
User
Team
ProjectModelingProgress
ModelingTask
```

当前生成指引的口径：

- 已完成 / 已完结项目不进入当前风险指引。
- 项目级风险会生成当前推进指引。
- 排期任务进入指引的条件：当前任务、近期关键路径任务、阻塞、超 3 天未更新、或近期延期风险。
- 建模待处理进入指引的条件：未分配款式、已送审、等反馈、修改卡住、建模中超 3 天未更新。
- `延期完成` 是历史结果，不作为当前风险任务进入待处理列表。
- 建议动作使用普通负责人可读文案，不直接展示脚本状态。
- 产品组工作指引可以写执行事实，但不修改项目计划上线日期、项目路线、项目等级、规划里程碑月份等规划基线。

基础版已验证：

- `npm run lint` 通过。
- `npm run build` 通过。
- `npx tsc --noEmit --pretty false` 通过。
- `/product-guide`、`/`、`/modeling`、`/users` 已浏览器打开验证，无控制台报错。
- 临时项目验证过任务写入、建模款式生成和 `/modeling` 渲染；验证后临时数据已清理。

基础版暂未做：

- 通知推送。
- AI 自动生成 SOP。
- 登录态下的默认项目组识别。
- 知识库 / SOP 链接正式接入。
- 产品组真实接入建模排期款式提交、任务启动、审核 / 送审结果。
- 项目排期真实读取建模排期可读完成事实。
- 标记原画过审动作。
- 正式 `待重新测算` 字段；当前只保留页面级提示口径。

## 产品组工作指引回写规则

产品组工作指引可以写执行事实，但写入的是其他模块的数据表，不独立制造另一套真相。

允许写入：

| 动作 | 写入对象 | 是否影响预测 |
|---|---|---:|
| 标记任务已完成 | `ProjectTask.status`、`actualFinishDate`、`ProgressUpdate` | 是 |
| 修改实际完成日期 | `ProjectTask.actualFinishDate`、`ProgressUpdate` | 是 |
| 填写预计完成日期 | `ProjectTask.expectedFinishDate`、`progressNote`、`ProgressUpdate` | 是 |
| 标记任务阻塞 | `ProjectTask.isBlocked`、`blockReason`、`ProgressUpdate` | 是 |
| 解除阻塞 | `ProjectTask.isBlocked`、`ProgressUpdate` | 是 |
| 录入款式清单 | 建模排期款式提交接口 | 是，影响建模排期 |
| 标记原画过审（后续） | `ModelingTask.originalArtStatus`、`originalArtApprovedDate` | 是，释放建模任务 |
| 添加普通备注 | `progressNote` | 否，只更新说明 |

说明：上表是完整业务口径；当前基础版已落地的是标记完成、预计完成日期、阻塞 / 解除阻塞、录入款式清单。标记原画过审、正式重新测算标记、SOP / 知识库联动还没做。

完成任务的强规则：

```text
工作指引里所有“完成”按钮都必须写入实际完成日期。
默认实际完成日期 = 今天，允许手动修改。
```

保存后建议流程：

```text
写入执行事实
→ 记录 ProgressUpdate
→ 标记需要重新测算
→ 页面提示“已有进度更新，建议重新测算”
→ 管理层或负责人点击重新测算
```

第一版不建议每次保存都自动测算，避免频繁计算和误操作。

## 款式清单与建模排期边界

款式清单源头：

```text
产品负责人在产品组工作指引中录入。
```

写入对象：

```text
通过建模排期接口生成或更新 ModelingTask
```

建模排期输入：

- 项目排期给出的建模窗口
- 产品组工作指引录入的款式清单
- 产品美术确认的原画过审状态
- 用户数据中的建模师、产能、擅长类型
- 用户数据中的稳定外包供应商
- 款式难度和预计工时

建模排期写入：

- 分配建模师
- 标记外包
- 计划开始 / 完成日期
- 建模状态
- 实际开始 / 完成日期
- 剩余工时
- 修改反馈

项目排期里的「根据效果图建模」读取规则：

```text
如果项目存在必做 ModelingTask：
  所有必做款式 = 已通过
  → 建模排期返回 canProjectScheduleTreatModelingDone = true
  → 建模排期返回 lastRequiredStyleApprovedDate = 最后一款通过日期
  → 项目排期自行决定是否写执行事实、是否重新测算

如果还有任意必做款式未通过：
  建模排期返回 canProjectScheduleTreatModelingDone = false
  项目排期不能把任务 7 / 10 建模事实视为已完成
```

边界原则：

```text
产品组创建款式。
建模排期推进款式。
项目排期主动读取款式结果并自行预测。
```

## 产品组工作指引动作生成

产品组工作指引不是简单任务列表，而是把系统信号翻译成产品组能执行的动作。

动作清单来源：

- 项目排期：当前关键任务、风险等级、预测完成日期、延期原因
- 项目任务：实际完成、预计完成、是否阻塞、最后更新时间
- 建模排期：未分配款式、送审、等反馈、修改、全部通过日期
- 用户数据：项目组、产品负责人、产品美术、人员归属
- 规划调整：管理层提前的里程碑、计划调整要求

每日动作必须包含：

- 当前关键任务未完成
- 任务有延期风险或必然延期
- 进行中超过 3 天未更新
- 缺少预计完成日期
- 管理层提前里程碑
- 款式清单未录入
- 原画已到计划完成窗口但未过审
- 建模款式未分配
- 建模卡在送审 / 等反馈 / 修改
- 项目阻塞

动作优先级：

```text
紧急：必然延期 / 阻塞 / 管理层提前要求
重要：延期风险 / 超 3 天未更新 / 建模卡住
本周处理：本周应完成 / 缺少预计完成日期 / 未录入款式
提前准备：下周准备 / 普通跟进
```

动作关闭规则：

```text
动作不是单独关闭，而是由底层事实变化自动消失。
```

例如：

- 任务已完成 → 任务动作消失
- 预计完成日期已填写 → 缺少预计时间动作消失
- 款式清单已录入 → 录入款式动作消失
- 款式已分配 → 未分配动作消失
- 阻塞解除 → 阻塞动作消失

## 数据导入当前状态

P0 数据导入先走统一入口，不再让各模块各自解析 Excel。

当前已实现：

- `/imports` 数据导入预览页
- `POST /api/imports/preview` 只读预览接口
- `POST /api/imports/apply` 项目主数据合并补充写入接口
- 第一版仅支持项目主数据 Excel，格式参考“番茄项目规划信息收集”
- 预览会显示已匹配、待新增、需确认、不可导入、基础资料新增、月度上线数量异常
- 预览不会写入数据库；确认导入会写入 `Project` 和 `DataImport`
- 确认导入不会静默触发排期重算；导入成功后页面提供“一键重算排期”按钮

2026-05-28 本地验证结果：

- 使用 `番茄项目规划信息收集 (10).xlsx`
- 预览 44 行项目主数据，全部匹配现有项目
- 确认导入新增 0 个项目、更新 44 个项目
- 手动触发排期重算成功：44 个项目、690 条未来任务
- `/imports` 和 `/` 页面均可访问
- `/imports` 已补充导入后的“一键重算排期”入口

2026-05-28 数据录入基线：

- 录入记录见 `docs/数据录入记录-2026-05-28.md`
- 最新项目数据使用 `番茄项目规划信息收集 (12).xlsx`
- 当前数据库基线：44 个项目、1324 条项目任务事实、674 张任务卡、24 条提醒
- 用户数据：68 个用户，其中 63 个启用；`fishoil` 已合并到真实人员 `于游`
- 建模数据：105 条真实建模款式任务、14 条项目建模进度、2 个外包供应商
- 已通过 `npm run lint`、`npm run build`、项目排期页和建模排期页访问验证

后续导入写库必须继续遵守：

- 先预览，再确认写入
- Excel 只能写输入数据，不能写预测完成、风险等级、超载判断等系统计算结果
- 普通导入默认合并补充；全量替换只允许 admin / 总经理执行
- 影响排期输入的数据写入后，必须提示或触发统一排期内核重算

## 当前开发原则

新对话接手时应遵守：

- 先读本文件和对应模块上下文，不要重新猜业务规则
- 不要一次横跨多个模块做大改
- 每个模块独立验收
- 修改排期、财务、送审相关逻辑时必须明确口径
- 所有关键页面修改后要跑 `npm run lint` 和 `npm run build`
- 前端视觉改动后要在浏览器里验证
- 真实业务样本优先用小鸟4代、自嘲熊、猫福珊迪2等已讨论过的项目做回归检查

## 建议的新对话拆分方式

每个新对话只做一个模块。例如：

```text
请阅读 docs/项目管理平台-当前总控上下文.md
请阅读 docs/模块任务上下文-项目排期与上线日历.md
本轮只处理项目排期与上线日历模块，不处理建模排期和财务测算。
```

模块完成后，应输出一份交接记录，至少包含：

- 做了什么
- 改了哪些文件
- 改了哪些业务口径
- 验证了哪些真实项目
- 还有哪些问题没做

## 2026-05-29 用户数据权限角色与导出中控记录

详见：

```text
docs/模块交接总结-用户数据权限角色与导出-20260529.md
```

中控确认口径：

- 用户数据模块唯一输入是用户数据 Excel。
- 用户数据模块只负责维护人员、团队、账号、权限角色、外包供应商等用户数据文档。
- 用户数据模块不主动向项目排期、产品组工作指引、建模排期、财务、知识库、送审 AI 输出业务结果。
- 其他模块如需人员、团队、权限，只能按需读取用户数据主数据。
- `PermissionRole`、标准 Excel 的 `固定字段/权限` 可作为登录与权限系统的共享基础字典保留。
- 不在用户数据模块维护模块权限矩阵；是否允许某个角色访问具体业务模块，由该业务模块自行判断。
- P0 内部测试阶段，业务模块优先保持粗粒度权限：`admin / manager / viewer`。
- 用户数据模块后续只继续做 Excel 导入/导出、账号登录、用户数据维护和验证，不继续承接跨模块权限联动。

## 2026-05-29 staging 提交权限中控记录

详见：

```text
docs/模块分支开发规范.md
```

中控确认口径：

- `staging` 是开发中控专用集成分支。
- 未来只有开发中控可以在 `staging` 上 commit 和 push。
- 各模块开发对话只能从 `staging` 新建自己的 `feature/*`、`fix/*` 或 `chore/*` 分支。
- 各模块开发对话只能在自己的模块分支或独立 worktree 中开发。
- 各模块开发完成后，提交自己的模块分支并输出交接总结。
- 是否合并到 `staging`，由开发中控统一判断和执行。
- 如果模块对话发现自己已经在 `staging` 上且有改动，必须停止，不提交、不回退、不切分支，先把 `git status` 发给开发中控。
