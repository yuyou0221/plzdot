# 模块交接总结：用户数据 Excel、登录与权限字典

最后更新：2026-05-29

项目路径：

```text
C:\Users\yuyou\Documents\Codex\2026-05-21\new-chat\project-management-platform
```

当前分支：

```text
staging
```

## 本轮模块边界

用户数据模块是平台的人和组织主数据模块，但本轮边界已经收窄：

- 唯一输入：标准用户数据 Excel。
- 唯一系统功能：提供登录功能。
- 负责维护：人员名单、团队结构、岗位/业务角色、登录账号、权限字典、建模师每周可用工作日、外包供应商。
- 不负责：项目排期、建模排期、产品组工作指引、财务、知识库、送审 AI 的业务逻辑。
- 不主动向其他模块输出业务结果，不触发跨模块联动写入。
- 其他模块需要人员、团队、账号或权限时，自行读取用户数据主数据。
- 某个模块是否允许特定角色访问，由该模块自行判断；用户数据模块不维护模块访问矩阵。

模块边界已记录在：

```text
docs/模块开发边界上下文.md
```

## 标准 Excel

用户维护和上传的标准文件：

```text
C:\Users\yuyou\Desktop\项目管理资料\04用户数据导入模板.xlsx
```

当前识别工作表：

- `人员名单`
- `固定字段`
- `团队结构`
- `外包供应商`

`固定字段` 当前只作为权限字典来源，核心列为：

```text
权限
```

不再使用“项目排期 / 产品组工作指引 / 建模排期 / 用户数据”等模块权限矩阵列。

## 导入规则

当前用户数据导入为覆盖更新：

- 只支持 `.xlsx`。
- 每次导入都会按 Excel 覆盖当前用户数据。
- 有 ID 时优先按 ID 匹配。
- 没有 ID 时，人员按姓名辅助匹配，团队按团队名称辅助匹配，外包供应商按供应商名称辅助匹配。
- Excel 中缺失的人员、团队、外包供应商不会物理删除，会统一停用。
- `初始/重置密码` 会写入登录密码哈希，并保存一份可导出的加密副本。
- 数据库不裸存明文密码；admin 导出时可从加密副本还原到 Excel。

导入接口：

```text
POST /api/users/import-excel
```

当前允许导入角色：

```text
admin, manager
```

## 导出规则

导出接口：

```text
GET /api/users/export-excel
```

当前仅 `admin` 可导出。

导出 Excel 工作表：

- `人员名单`
- `固定字段`
- `团队结构`
- `外包供应商`

导出的 `人员名单` 会包含 `初始/重置密码` 列。只有曾通过本系统导入或重置过明文密码的账号，才能还原导出密码；历史上只保存哈希的旧账号无法反推出明文。

## 已做代码变更

数据库字段：

- `User.passwordExportCiphertext`

新增数据表：

- `PermissionRole`

当前 `PermissionRole` 只作为权限角色/权限字典表，不保存模块访问矩阵。

相关迁移：

```text
prisma/migrations/20260528113500_add_password_export_ciphertext/
prisma/migrations/20260528152000_add_permission_roles/
prisma/migrations/20260529013000_simplify_permission_roles/
```

核心代码文件：

```text
src/lib/user-data-import.ts
src/lib/user-data-export.ts
src/app/api/users/import-excel/route.ts
src/app/api/users/export-excel/route.ts
src/components/users/user-data-workbench.tsx
src/lib/auth/password-export.ts
src/lib/auth/permissions.ts
src/lib/auth/default-admin.ts
scripts/import-user-data-json.ts
```

## 页面入口

用户数据页面：

```text
/users
```

页面当前提供：

- 用户数据列表查看。
- 用户数据搜索和筛选。
- Excel 导入入口。
- Excel 导出入口。
- 导入说明：每次导入为覆盖更新。

当前页面已经把手工新增/编辑入口关闭，用户数据只通过 Excel 更新。

## 已验证内容

本地已完成：

```text
npx prisma format
npm run db:generate
npx prisma migrate deploy
npm run lint
npm run build
```

本地也用构建后的服务验证过：

- 健康接口可访问。
- 用户数据导出接口可生成 Excel。
- 导出工作表为 `人员名单 / 固定字段 / 团队结构 / 外包供应商`。
- `固定字段` 导出列为 `权限`。

## 上传 / 部署注意

服务器更新后需要执行：

```bash
git pull --ff-only origin staging
npm ci
npm run db:generate
npm run db:migrate
npm run build
pm2 restart project-management-staging
```

数据库迁移会新增可导出密码密文字段和权限字典表，并把早期权限矩阵字段移除。

服务器环境需要确保：

```text
AUTH_SECRET
PASSWORD_EXPORT_SECRET
DATABASE_URL
```

其中 `PASSWORD_EXPORT_SECRET` 用于可导出密码的加密/解密。若没有配置，会回退使用 `AUTH_SECRET` 或 `DATABASE_URL` 派生密钥，但正式/测试服务器建议单独配置稳定密钥。

## 当前风险

当前本地工作区除了用户数据模块外，还存在排期/产品指引相关文件改动和一个本地领先提交：

```text
c3f1a6e fix: apply planned launch date rules
```

这些内容不属于用户数据模块边界。上传 `staging` 前应确认是否允许一起进入服务器；如果只上传用户数据模块，应单独拆分提交，避免把边界外改动混入本轮更新。

## 还没做

- 还没有把用户提供的最新 Excel 正式导入服务器数据库。
- 还没有在服务器页面上完成导入/导出全流程验收。
- 还没有做请假 / 不可排期记录的 Excel 化维护。
- 还没有让其他模块正式读取用户数据；后续如需要，由各模块自行读取并判断访问权限。
