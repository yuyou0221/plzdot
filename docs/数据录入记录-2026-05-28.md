# 数据录入记录 - 2026-05-28

本记录用于说明 2026-05-28 本地数据库按 P0 新数据格式完成的数据录入情况。

## 录入前备份

已在录入前生成本地数据库备份：

```text
.local/backups/before-data-entry-20260528-004549.sql
```

## 本次录入范围

本次只处理当前 P0 已有 schema 能承接的数据：

- 项目主数据
- 项目任务事实
- 排期测算结果
- 用户组织基础数据整理
- 建模款式任务
- 项目建模进度

暂未处理：

- 结构化版权方表
- 结构化 IP 表
- 结构化产品类型表
- 用户业务角色表
- 建模分配历史表
- 任务进度事件表

这些表在当前 Prisma schema 中尚未完成迁移，暂时仍由现有字段或日志承接。

## 项目主数据

来源文件：

```text
C:\Users\yuyou\Downloads\番茄项目规划信息收集 (12).xlsx
```

处理结果：

```text
预览项目行：44
已匹配：44
新增：0
冲突：0
不可导入：0
未校验：0
警告：2
```

导入结果：

```text
导入批次：319e3d7d-5bbf-44a4-a51d-f27bd036b747
新增项目：0
更新项目：44
```

补充修正：

- 最新 Excel 末尾存在只有“所属团队=丹东组”的空项目行。
- 已调整项目主数据预览逻辑：这类缺少项目名称、编号、版权方、IP、产品类型、上线日期的空项目行会被忽略，不再阻断导入。

## 项目任务事实与排期结果

来源文件：

```text
C:\Users\yuyou\Downloads\番茄项目规划信息收集 (12).xlsx
```

处理方式：

```text
POST /api/schedule/import-excel
today=2026-05-28
```

导入结果：

```text
导入批次：actual-import-1779900594291
项目数：44
项目任务事实：1324
未来任务：674
项目级排期结果：44
任务级排期结果：1324
任务卡：674
提醒：24
```

注意：

```text
unmatchedActualCount = 3
```

未匹配样本均为空任务名 / 空 recordKey：

- 小狗mini3
- 鲨猫
- 鲨猫

当前判断为实际进度表中的空行或无效行，不是有效任务事实。

## 用户组织数据

当前库内结果：

```text
用户总数：68
启用用户：63
停用用户：5
团队总数：21
登录账号：4
```

已整理：

- 将 `fishoil` 登录账号合并到真实人员 `于游`。
- `于游` 当前为 admin，总经理，团队为 IP运营。
- 原重复 `于游` 账号记录已停用。

当前登录名 / 权限角色：

```text
admin / admin
fishoil / admin
chanpin / manager
jianmo / manager
```

说明：

- 这里只记录账号和权限角色，不记录密码。
- 仍保留停用记录，未做物理删除。

## 建模款式数据

来源文件：

```text
C:\Users\yuyou\Documents\Codex\2026-05-21\new-chat\project-management-platform\outputs\modeling-export-20260525\建模款式导出-20260525-服务器上传.xlsx
```

中间转换：

```text
.local/imports/modeling-tasks-20260528.json
```

导入结果：

```text
导入批次：modeling-data-import-1779900604832
源数据行数：105
创建建模款式任务：105
跳过行：0
反馈记录：0
项目建模进度：14
外包供应商：2
```

## 当前数据库基线

最终检查结果：

```text
Project：44
ProjectTask：1324
ScheduleRun：1
ScheduleProjectResult：44
ScheduleTaskResult：1324
TaskCard：674
Alert：24
User：68
Team：21
ModelingTask：105
ProjectModelingProgress：14
OutsourceVendor：2
```

## 验证

已通过：

```text
npm run lint
npm run build
GET /
GET /modeling
```

页面验证：

- 项目排期页可访问
- 建模排期页可访问

## 后续建议

下一步适合继续做：

1. 用户组织导入预览器，避免用脚本直接导用户。
2. 建模款式导入预览器，显示将创建 / 跳过 / 覆盖哪些款式。
3. 把 `unmatchedActualCount` 的空行过滤逻辑补进排期内核导入报告。
4. 提交 git 并部署 staging，再在服务器上跑同一套数据录入。
